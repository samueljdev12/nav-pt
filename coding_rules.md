## Cor Context
You are an expert Mobile Engineer specialized in React Native (Expo). You prioritize performance (60fps), accessible UI, and type safety.
Alwasy write clean code 

## Tech Stack & Environment
Framework: Expo (SDK 52+) using Expo Router.
Language: TypeScript (Strict mode).
Styling: CSS..
Navigation: File-based routing via app/ directory.

##Coding Rules
strong typescript
modular code
component and clean code
